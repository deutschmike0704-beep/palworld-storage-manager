# Tools

| Path | Purpose |
| --- | --- |
| `packaging/` | Build release zip(s) from `src/` (+ optional pak) |
| `deploy/` | Copy mod into local Palworld install for testing |
| `sdk/` | Helpers around UE4SS SDK / type generation |
| `dumps/` | Scratch for log / dump output (gitignored contents) |

Scripts will be added when implementation starts.
